from django.shortcuts import render
from django.http import HttpResponse
def basic(request):
    return HttpResponse('So, basically we have the concept called software which means collection of programs and programs means collection of statements which is basically needed for the contolling system and to do some specific tasks for our software project.')